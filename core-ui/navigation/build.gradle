plugins {
    alias libs.plugins.android.library
    alias libs.plugins.kotlin.android
}

android {
    compileSdk 32

    defaultConfig {
        minSdk 23
        targetSdk 32

        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
        consumerProguardFiles "consumer-rules.pro"
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    buildFeatures {
        compose true
    }
    composeOptions {
        kotlinCompilerExtensionVersion libs.versions.androidx.composeCompiler.get()
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }
    kotlinOptions {
        jvmTarget = '1.8'
    }
}

dependencies {
    implementation(project(":core-ui:screens:auth:welcome"))
    implementation implementation(project(':core-ui:screens:auth:login-register'))
    implementation(project(":core-ui:screens:main:base"))
    implementation(project(":core-ui:screens:main:home"))
    implementation(project(":core-ui:screens:main:explore"))
    implementation(project(":core-ui:screens:main:favorites"))
    implementation(project(":core-ui:screens:main:profile"))
    implementation(project(":core-ui:screens:main:seeall"))

    implementation(project(":core-ui:theme"))
    implementation(project(":core-ui:utils"))
    implementation(project(":domain:domain-usecase"))
    implementation(project(":features:auth:firebase:firebase-api"))

    implementation(libs.bundles.androidx.compose)
    implementation(libs.bundles.voyager)
    implementation(libs.koin.core)
    implementation(libs.koin.compose)

    implementation(libs.bundles.androidx.core)
    implementation(libs.androidx.appcompat)
    implementation(libs.google.android.material)
    testImplementation(libs.test.junit4)
    androidTestImplementation(libs.bundles.test.android)
}